#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias docker='podman'
PS1='[\u@\h \W]\$ '

# Added by GDK bootstrap
eval "$(/home/junevm/.local/bin/mise activate bash)"